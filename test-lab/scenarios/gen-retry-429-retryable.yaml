# GENERATED by test-lab/generate-matrix.py — do not edit by hand.
{
  "name": "gen-retry-429-retryable",
  "description": "retry {'maxAttempts': 2, 'retryableStatusCodes': [429], 'delayMs': 20, 'backoffMultiplier': 1, 'maxDelayMs': 50} against /reliability/destination/always-429 must issue exactly 3 request(s).",
  "pipeline": "test-lab/pipelines/gen-retry-429-retryable.yaml",
  "expect_status": "error",
  "setup": {
    "reset_db": false
  },
  "assertions": [
    {
      "http_count_eq": {
        "method": "POST",
        "path": "/reliability/destination/always-429",
        "expected": 3
      }
    },
    {
      "log_not_contains": "panic:"
    }
  ]
}
