# GENERATED by test-lab/generate-matrix.py — do not edit by hand.
{
  "name": "gen-chain-split-then-toint-fails",
  "description": "A chain fails when a later op cannot convert the previous output.",
  "pipeline": "test-lab/pipelines/gen-chain-split-then-toint-fails.yaml",
  "expect_status": "error",
  "setup": {
    "reset_db": false
  },
  "assertions": [
    {
      "log_contains": "cannot convert"
    },
    {
      "log_not_contains": "panic:"
    }
  ]
}
