# GENERATED by test-lab/generate-matrix.py — do not edit by hand.
{
  "name": "gen-retry-500-not-in-retryable-list",
  "description": "retry {'maxAttempts': 3, 'retryableStatusCodes': [503], 'delayMs': 20, 'backoffMultiplier': 1, 'maxDelayMs': 50} against /reliability/destination/always-500 must issue exactly 1 request(s).",
  "pipeline": "test-lab/pipelines/gen-retry-500-not-in-retryable-list.yaml",
  "expect_status": "error",
  "setup": {
    "reset_db": false
  },
  "assertions": [
    {
      "http_count_eq": {
        "method": "POST",
        "path": "/reliability/destination/always-500",
        "expected": 1
      }
    },
    {
      "log_not_contains": "panic:"
    }
  ]
}
