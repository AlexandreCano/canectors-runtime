# GENERATED by test-lab/generate-matrix.py — do not edit by hand.
{
  "name": "gen-transform-toobject-from-string",
  "description": "mapping transform toObject on mixed cannot convert and fails the pipeline under the default onError=fail.",
  "pipeline": "test-lab/pipelines/gen-transform-toobject-from-string.yaml",
  "expect_status": "error",
  "setup": {
    "reset_db": false
  },
  "assertions": [
    {
      "log_contains": "cannot convert string to object"
    },
    {
      "log_not_contains": "panic:"
    }
  ]
}
