# GENERATED by test-lab/generate-matrix.py — do not edit by hand.
{
  "name": "gen-retry-503-body-hint-true",
  "description": "retry {'maxAttempts': 2, 'retryableStatusCodes': [503], 'retryHintFromBody': 'body.retryable == true', 'delayMs': 20, 'backoffMultiplier': 1, 'maxDelayMs': 50} against /reliability/destination/always-503-retryable must issue exactly 3 request(s).",
  "pipeline": "test-lab/pipelines/gen-retry-503-body-hint-true.yaml",
  "expect_status": "error",
  "setup": {
    "reset_db": false
  },
  "assertions": [
    {
      "http_count_eq": {
        "method": "POST",
        "path": "/reliability/destination/always-503-retryable",
        "expected": 3
      }
    },
    {
      "log_not_contains": "panic:"
    }
  ]
}
