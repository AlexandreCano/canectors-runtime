name: volume-offset
description: "Offset pagination at volume — 3 pages of 1 000 must all arrive."
pipeline: test-lab/pipelines/volume-offset.yaml
expect_status: success
timeout: 60
records_expected: 3000
setup:
  reset_db: false
assertions:
  - log_not_contains: "panic:"
