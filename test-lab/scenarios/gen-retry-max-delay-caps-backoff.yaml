# GENERATED by test-lab/generate-matrix.py — do not edit by hand.
{
  "name": "gen-retry-max-delay-caps-backoff",
  "description": "retry {'maxAttempts': 2, 'retryableStatusCodes': [500], 'delayMs': 40, 'backoffMultiplier': 10, 'maxDelayMs': 45} against /reliability/destination/always-500 must issue exactly 3 request(s).",
  "pipeline": "test-lab/pipelines/gen-retry-max-delay-caps-backoff.yaml",
  "expect_status": "error",
  "setup": {
    "reset_db": false
  },
  "assertions": [
    {
      "http_count_eq": {
        "method": "POST",
        "path": "/reliability/destination/always-500",
        "expected": 3
      }
    },
    {
      "log_not_contains": "panic:"
    }
  ]
}
